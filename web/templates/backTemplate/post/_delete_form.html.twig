<form method="post" action="{{ path('app_post_delete', {'idpost': tbl_post.idpost}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline-block;">
<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_post.idpost) }}">
    <button class="btn btn-danger">Delete</button>
</form>
