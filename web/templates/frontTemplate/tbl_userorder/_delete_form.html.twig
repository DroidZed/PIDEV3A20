<form method="post" action="{{ path('app_tbl_userorder_delete', {'numberorder': tbl_userorder.numberorder}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_userorder.numberorder) }}">
    <button class="btn">Delete</button>
</form>
