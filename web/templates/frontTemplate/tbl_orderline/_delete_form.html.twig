<form method="post" action="{{ path('app_tbl_orderline_delete', {'idorderline': tbl_orderline.idorderline}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_orderline.idorderline) }}">
    <button class="btn">Delete</button>
</form>
