<!DOCTYPE html>

<title>Edit TblOrderline</title>

{% block body %}
    <h1>Edit TblOrderline</h1>

    {{ include('tbl_orderline/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_tbl_orderline_index') }}">back to list</a>

    {{ include('tbl_orderline/_delete_form.html.twig') }}
{% endblock %}
